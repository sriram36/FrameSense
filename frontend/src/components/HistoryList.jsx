import { API } from "../api.js";
export default function HistoryList({ items, onSelect, loading, error }) { return <section className="history-section"><div className="history-header"><div><p className="section-kicker">ARCHIVE / 02</p><h2>Analysis history</h2><p className="muted">Review previously inspected frames and their quality signals.</p></div><span className="history-count">{items.length} {items.length === 1 ? "result" : "results"}</span></div>{loading ? <div className="history-state"><div className="log-spinner" /><p>Loading analysis archive...</p></div> : error ? <p className="error-text"><strong>Couldn&apos;t load history.</strong> {error}</p> : items.length === 0 ? <div className="history-state"><span className="empty-mark">—</span><p>No analyses yet. Upload an image to get started.</p></div> : <div className="history-list"><div className="history-columns"><span>Frame</span><span>Score</span><span>Status</span><span>Captured</span></div>{items.map((item) => <button key={item.id} className="history-row" onClick={() => onSelect(item.id)}><div className="history-thumb-wrapper"><img src={API + item.image_url} alt="" className="history-thumb" /></div><span className="history-filename">{item.filename}</span><span className="history-score">{item.quality_score}</span><span className={`history-label label-${item.quality_label?.toLowerCase()}`}>{item.quality_label}</span><span className="history-date">{new Date(item.created_at).toLocaleString()}</span><span className="row-arrow">→</span></button>)}</div>}</section>; }
