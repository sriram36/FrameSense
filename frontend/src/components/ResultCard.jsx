import IssueBadge from "./IssueBadge.jsx";
import ScoreGauge from "./ScoreGauge.jsx";
const FEATURE_LABELS = { sharpness: "Sharpness", brightness: "Brightness", contrast: "Contrast", noise_estimate: "Noise estimate", saturation: "Saturation", blockiness: "Blockiness", edge_density: "Edge density" };
export default function ResultCard({ result, imageUrl, heatmapUrl }) { const features = Object.entries(result.features || {}); return <section className="result-card"><div className="result-heading"><div><p className="section-kicker">INSPECTION REPORT</p><h2>{result.filename || "Analyzed frame"}</h2></div><span className="result-id">RESULT / {String(result.id || "LIVE").slice(0, 8)}</span></div><div className="result-top"><div className="image-comparison">{imageUrl && <div className="image-container"><span className="image-label">Original frame</span><img src={imageUrl} alt={result.filename} className="result-image" /></div>}{heatmapUrl && <div className="image-container"><span className="image-label">Defect heatmap</span><img src={heatmapUrl} alt="Anomaly heatmap" className="result-image" /></div>}</div><ScoreGauge score={result.quality_score} label={result.quality_label} /></div><div className="result-section"><h3>Detected issues <span>{result.issues.length}</span></h3>{result.issues.length === 0 ? <p className="muted success-note">✓ No issues detected in this frame.</p> : <div className="issue-list">{result.issues.map((issue, i) => <IssueBadge key={i} issue={issue} />)}</div>}</div><div className="result-section"><h3>Image statistics <span>{features.length} signals</span></h3><div className="feature-grid">{features.map(([key, value]) => <div key={key} className="feature-item"><span className="feature-label">{FEATURE_LABELS[key] || key}</span><span className="feature-value">{typeof value === "number" ? value.toFixed(3) : value}</span></div>)}</div></div></section>; }
