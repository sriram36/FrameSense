# Only used by `npm run dev` (the Vite dev server) to know where to proxy
# /api requests during local development. Not used in the Docker build --
# the production nginx config proxies /api to the backend container by
# service name instead, so no rebuild is needed to point at a different
# backend host.
VITE_DEV_API_TARGET=http://localhost:8000
