fastapi==0.115.*
uvicorn[standard]==0.32.*
sqlalchemy==2.0.*
pydantic==2.*
python-multipart==0.0.*
opencv-python-headless==4.10.*
numpy==2.*
scikit-learn==1.5.*
joblib==1.4.*

# Optional: only needed for the conv autoencoder (app/ml/autoencoder.py,
# train/train_autoencoder.py). The PCA-based anomaly detector
# (app/ml/pca_anomaly.py) needs only the packages above and is used
# automatically as a fallback if this isn't installed or hasn't been
# trained -- see app/ml/inference.py. Install this for the higher-capacity
# nonlinear model; the app runs fully without it.
torch==2.4.*

# test-only, but small enough to keep in one file for simplicity
pytest==8.*
httpx==0.27.*

